# Saiwu 面试对话树手册

> **核心认知：面试官只知道你说出来的东西。**
> 他没读过你的代码，不知道 planner、审批开关、replan 的存在。
> 每一个问题都来自你上一句话里的某个名词——这是一场"你出钩子、他咬钩子"的游戏。
> 所以准备的重点不是"背答案"，而是：**每个回答埋什么钩子、钩子被咬后怎么接、接住后能往下守几层。**

---

## 0. 三条纪律

1. **只说你扛得住三层追问的名词。** 说出口的每个术语都是邀请函——"混合检索"邀请"为什么两路"，"0.7/0.3"邀请"怎么定的"。没把握的词不出口。
2. **项目内部黑话不主动说。** "审批开关""replan""Mermaid 卡片"这些词面试官没有概念，说了要么没人接（浪费），要么引偏（先花 30 秒解释概念）。它们是**被动弹药**——对话走到那才用（见第五节）。
3. **每个回答的结尾就是下一个钩子。** 答完"权重是跑评测定的"，面试官自然会问"评测怎么建的"——你想要的下一个问题，应该藏在上一个回答的最后一句里。

---

## 1. 开场：唯一主动权完全在你手里的时刻

**Q：介绍一下你的项目。**（必考，而且是定调时刻——你说什么决定了整场面试聊什么）

**标准版（约 40 秒）：**

> 我做了一个本地优先的个人知识库智能体。用户把 PDF、Word、飞书文档等资料丢进去，提问时系统做**混合检索**——向量加关键词两路召回——再由大模型生成**带可点击引用**的回答，每个结论都能点回原文。技术上后端是 FastAPI 加 LangGraph 多智能体编排，存储用 Chroma 向量库加 SQLite，另外自己实现了 **MCP 客户端**让模型能调用外部工具。

**逐句拆钩子（每个加粗词都是你故意放的）：**

| 你说的词 | 面试官大概率接的问题 | 你守得住吗 |
|---|---|---|
| 混合检索 | 为什么两路？怎么融合？ | ✅ 主场，能守四层 |
| 带可点击引用 | 怎么防幻觉？ | ✅ 有现成答案 |
| LangGraph 多智能体 | 怎么编排？为什么用它？ | ✅ 主场 |
| MCP 客户端 | 通信怎么做的？踩过坑吗？ | ✅ 最有区分度的故事 |
| 本地优先 | 为什么本地？ | ⚠️ 只答"个人数据敏感+零运维成本"，别展开 |

**故意不说的**：0.7/0.3 权重、切片参数、db/query 双模式、replan 循环——这些是纵深弹药，留给追问时展示，开场说等于把底牌全摊开还稀释了重点。

---

## 2. 主线 A：RAG 线（只要项目叫"知识库"，这条线必走）

**Q：你的 RAG 是怎么做的？**（第一枪几乎总是这个）

**标准答（30 秒，埋三个钩）：**

> 离线侧：文档解析后切成带重叠的文本块，用 embedding 模型向量化，存进 Chroma 向量库。在线侧：用户问题也做向量化，同时并行走一路关键词检索，两路结果加权融合取 top-K，作为参考材料塞给大模型生成回答。

钩子：**embedding**、**关键词那一路**、**切**。下面是三棵子树。

### 子树 A1：embedding（顺着"向量化"必被咬）

```
「embedding 向量化」
├─ 追问1：为什么要用模型做向量化？关键词检索不行吗？（概率：高）
│   答：检索的本质是匹配意思而不是匹配字面。用户问"牛魔王的来历"，
│       文档里写的是"平天大圣"——字面零重合，关键词检索直接翻车，
│       这是我们项目里真实踩过的坑。Embedding 把语义压缩成向量，
│       语义相近 = 余弦距离近，查"来历"能命中写"身世"的文档。
│
├─ 追问2：那语义检索都有了，为什么还要关键词那一路？（概率：高）
│   答：向量对专有名词、编号、精确术语反而弱（embedding 倾向"大概意思"）。
│       比如查"牛魔王"三个字，向量可能匹配到任何妖怪；FTS5 关键词检索
│       能精确咬住这四个字。两路互补，按 (note_id, chunk_index) 去重后
│       加权融合：0.7 向量 + 0.3 关键词。
│       └─ 埋钩：「权重是跑评测定的」→ 引出评测集（见 A4）
│
├─ 追问3：权重 0.7/0.3 怎么定的？拍脑袋？（概率：中高）
│   答：不是。建了金标准评测集，跑 Recall@K 和 MRR，调权重、重跑、
│       对比，最后定在 0.7/0.3。这个循环本身就是项目的评测方法论。
│       （若追问"为什么不用 RRF"：RRF 更稳健但无权重可调；个人库规模下
│       加权融合 + 评测调参收益更直接。）
│
├─ 追问4：用的什么 embedding 模型？费钱吗？（概率：中）
│   答：MiniMax embo-01，入库用 db 模式、查询用 query 模式——文档和
│       查询的编码空间不同，混用会掉点。成本上 embedding 是整条管线
│       最便宜的一环：入库每篇只算一次，查询每次只有一句话；
│       真正的大头是生成回答的 LLM，每次要吃几千 token 上下文。
│
└─ 追问5：中文检索有什么特殊处理？（概率：中，尤其面试官做过搜索）
    答：FTS5 那路做了三轮降级——短语查询 → 拆词 OR 查询 → LIKE 回退，
        因为中文分词对未登录词（人名、昵称）几乎无能为力。
```

### 子树 A2：切片（顺着"切"被咬）

```
「切成带重叠的文本块」
├─ 追问1：怎么切的？多大？（概率：高）
│   答：500 字一块、80 字重叠，但不是硬切——优先在段落和句子边界落刀，
│       找不到合适边界才硬切。
│
├─ 追问2：为什么是固定长度？不按语义/结构切？（概率：高——这是热门考点）
│   答（三层递进）：
│       第一层：确定性。chunk 的 (note_id, chunk_index) 是去重键、FTS 索引键、
│           增量同步的游标——同一篇文档今天切和明天切必须完全一致，否则
│           增量同步判定全量变更。语义切片、LLM 切片都做不到幂等。
│       第二层：预算可控。下游参考材料有 3000 token 总预算、单块 800 字上限，
│           固定 500 保证每块天然装得进预算；按章节切出一个 5000 字的块
│           还得二次截断，等于白切。
│       第三层：来源无关。11 种入库路径（PDF/OCR/飞书…），很多来源没有
│           可靠的结构信息，固定窗口只认文本不认结构，一条切法通吃。
│
└─ 追问3：80 重叠是干嘛的？（概率：中）
    答：跨边界句子两边都能被完整检索到——是 small-to-big 模式的轻量近似。
        重叠率刻意压在 16% 左右：重叠太高会产生语义重复的两个块双双命中，
        浪费预算。
```

### 子树 A3：引用与幻觉（顺着"带引用"或任何"幻觉"话题被咬）

```
「带可点击引用」
├─ 追问1：怎么保证不瞎编？（概率：高——幻觉是所有 LLM 应用必问）
│   答：三道闸：
│       ① 检索层——双阈值过滤低分块，弱相关的不进上下文；
│       ② 提示层——要求每个结论标 [n] 引用编号，前端点编号跳回原文块；
│       ③ 兜底层——参考材料为空或全不相关时，明确指示模型用自己的知识
│           回答并且不许提"参考资料"，避免"根据参考材料但材料里没有"
│           这种自曝式回答。
│
├─ 追问2：引用和原文块怎么对齐的？（概率：中）
│   答：LLM 在答案里写 [n] 标记，后端正则提取 + 编号合法性校验
│       （越界编号直接丢弃），前端把 [n] 渲染成可点击按钮。
│       承认这是已知技术债：正则提取依赖模型守格式，改进方向是
│       函数调用/结构化输出生成引用。
│
└─ 追问3：检索回来的东西质量差怎么办？（概率：中）
    答：多轮补查——检索结果不够时，廉价模型看已有材料生成补缺查询
        再检索一轮，最多三轮，凑够目标块数提前退出。有三道保险丝：
        够数即停、轮数封顶、查询重复即停。
```

### 子树 A4：评测（从 A1 追问3 自然流入；或被直接问"效果怎么衡量"）

```
「跑评测定的」
├─ 追问：评测集怎么建的？什么指标？
│   答：金标准集（问题 + 标准出处块），三个指标：
│       Recall@K（该召回的有没有召回）、MRR（正确块排多前）、
│       闲聊误引率（闲聊问题不该触发检索——考"知道什么时候不检索"）。
│       第三个指标是自己的设计：RAG 系统的失败不只是"检索不准"，
│       还包括"不该检索的时候检索了"。
```

---

## 3. 主线 B：Agent / 编排线（开场说了"多智能体/MCP"就会走）

**Q：多智能体？怎么个多法？**（咬"多智能体"这个词）

**标准答：**

> 核心是一个意图路由：用户问题先分类——闲聊、研究、入库、身份——不同意图走不同链路。闲聊直接回答不检索；研究类问题才走完整链路：先拆解成子查询，逐步检索，材料不够就补查，最后综合回答。

```
「意图路由」
├─ 追问1：分类用什么做？LLM？（概率：高）
│   答：LLM 分类，但有个重要教训：最早版本分类模型延迟 12 秒，后来把
│       "你好/谢谢"这类高频简单意图用 regex 快速通道前置分流，完全
│       不进 LLM——能用规则解决的不该用模型。这是个成本意识问题。
│
├─ 追问2：为什么闲聊不过检索？（概率：中高）
│   答：两个理由——延迟（闲聊等 2 秒检索体验很差）和质量（检索回一堆
│       不相关块反而污染回答，模型会硬往资料上靠）。知道什么时候
│       不检索和知道怎么检索一样重要。
│
└─ 追问3：拆解子查询的规划挂了怎么办？（概率：中，考容错意识）
    答：任何失败都回退到启发式追问循环，主流程永不阻塞——增强模块
        必须是可失败的可选项，不是必经之路。
```

```
「为什么用 LangGraph」（咬"LangGraph"这个词，概率：高）
答：三个硬需求——① 条件路由（意图分流）；② 循环（多轮补查，链式 DAG
    做不到）；③ 持久化 checkpoint（对话状态可恢复，进程重启不丢）。
追问"为什么不用 LangChain 链/自研"：链不支持循环；自研要自己解决
状态持久化和重放语义，成本高。
追问"checkpointer 怎么选的"：AsyncSqliteSaver，本地单机零运维；
分布式场景换 Postgres saver，接口已抽象。
```

### MCP 子树（最有区分度，咬"MCP 客户端"这个词；面试官普遍知道 MCP 是什么）

```
「自己实现了 MCP 客户端」
├─ 追问1：通信怎么实现的？（概率：高）
│   答：stdio，自己写的客户端。核心设计：一个进程一个常驻读线程 +
│       消息队列；同一轮对话复用一个 server 进程，进程死了自动重启；
│       工具清单不预加载，模型先调发现工具拿清单再决定调用。
│
├─ 追问2：为什么不用官方 SDK？（概率：中）
│   答：轻量可控，而且自己实现逼着理解协议本身——后来排 stdio 的
│       并发问题不用翻 SDK 源码。
│
└─ 追问3：踩过什么坑？（概率：极高——这是你主动放的最肥的钩子）
    答（四连，按精彩程度排序）：
    ① 协议理解错：MCP stdio 是换行分隔的 JSON，不是 HTTP 那种
       Content-Length 帧——照 LSP 习惯写就错了。
    ② 并发时序 bug（最难）：每次调用起一个新读线程，被废弃的线程会
       "偷走"后续请求的响应——症状随机交错，单步调试无效，只能靠
       推理并发模型修复：一进程一线程，队列挂在进程上。
    ③ Windows 三连：select() 等不了管道；npx 要解析成 npx.CMD；
       terminate() 只杀 cmd 壳，要 taskkill /F /T 杀整棵进程树。
    ④ 死代码：读消息循环写在 early return 后面。
```

---

## 4. 主线 C：Debug / 工程线（"项目中遇到最难的问题是什么？"）

**注意：这里主动权又回到你手里——你选故事。** 选故事的标准：面试官**不需要项目背景就能听懂冲突**。

**首选故事：幽灵 bug（任何人秒懂）**

> 上线后接口随机 500，报 `'RuleSet' object has no attribute 'strip'`。定位到一个配置函数返回了对象而不是字符串，被存进了状态，下游调字符串方法崩。修了，**但报错没停**。
> 最后发现：Windows 上 uvicorn `--reload` 没有真正重启 worker，旧进程一直在跑旧代码——我盯着正确的代码和持续复现的报错，差点怀疑人生。手动杀光所有残留进程重启才真正生效。
> 教训：**调试第一步先确认"磁盘上的代码 = 内存里的代码"**，进程考古优先于代码考古。

**次选：消息翻倍（被问数据一致性/幂等时）**

> LangGraph 的 `operator.add` reducer + 持久化 checkpointer，重放时历史消息翻倍——DB 种子数据叠加 checkpoint 重放历史。教训：持久化图里的 reducer 必须按"重放安全"的语义设计，add 这种累积语义和 checkpoint 重放天然冲突。

**三选：飞书同步（被问"数据管道/同步"时）**

> 初始同步时 embedding 失败（缺 API key），但同步逻辑把版本号推进了——这批文档永久卡在"待索引"，再也不会被重试。修复：只在成功后才推进版本号。教训：**增量同步的推进条件必须是"成功"而不是"处理过"**——失败被静默吞掉的增量同步等于数据丢失。

---

## 5. 被动弹药库（不主动说，对话走到才用）

| 弹药 | 触发条件 | 要点 |
|---|---|---|
| Plan / 审批开关 | 被问"agent 怎么决定拆解问题/有没有人工介入" | Plan 控制**是否分解**；审批控制**分解后是否要用户确认**才执行。两者只作用于研究类意图 |
| 上下文组装细节 | 被问"prompt 怎么拼的/token 怎么控制" | 固定顺序 system(人设+摘要+画像+参考)→历史→问题；参考材料 3000 token 预算低分先截断；历史 20 条/6000 token 滑窗，溢出压缩成 ≤300 字摘要 |
| 流式解析容错 | 被问"流式输出/前端渲染" | `</think>` 偏移量多吃首字符 bug；标签不闭合导致正则失配——修复是把闭合匹配改可选。经验：为不守格式的对手写解析器，正则必须宽容化 |
| Mermaid 炸弹 | 基本只有聊到前端渲染 bug 才用 | mermaid.render 失败把错误 SVG 留在 body 末尾的临时容器；流式期间未闭合围栏被 marked 当完整代码块反复渲染。修复：围栏奇偶门控 + 孤儿容器清扫 |
| 安全 | 被问"安全怎么考虑" | PBKDF2 密码哈希；API key 走 .env；MCP 破坏性工具走权限弹窗；server 显式 enabled 才暴露 |
| 工程规范 | 被问"工程规范/协作" | schema 变更必须配迁移+幂等守卫；i18n 强制；非平凡逻辑必须带测试 |

---

## 6. 掌舵技巧

**被问到不会的：** 诚实 + 给思路 + 给验证方法。
> "这个我没实测过。我的直觉是 X，因为 Y；如果要验证，我会先跑 Z 看数据。" ——比硬编强十倍，而且"直觉+验证方法"本身就是高级答案。

**把话题拉回主场：** 每个回答的结尾放一个通向主场的钩。主场三个领域（能守最深的）：**混合检索与评测**、**MCP stdio 细节**、**流式解析容错**。

**收尾主线（被问"这个项目最大的收获"）：**

> 最深的体会是：RAG 系统的难点不在算法，而在**管道两端约定不可靠**——模型不守格式、进程不守协议、框架不守重放语义、操作系统不守你的直觉。所以工程重心落在防御性解析、幂等守卫、可降级设计和评测驱动调参上。

**反问环节：**

- "团队检索链路目前是加权融合还是 rerank 为主？"（展示对业界的认知）
- "Agent 类功能的容错标准是什么？增强模块允许降级吗？"（呼应自己的设计哲学）
